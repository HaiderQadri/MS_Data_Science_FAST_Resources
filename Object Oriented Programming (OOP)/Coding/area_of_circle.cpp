#include <iostream>
using namespace std;

int main() //? Calculate the area of Circle having radius 20
{
    int r;
    float pie, area;
    pie = 3.14, r = 20;
    area = pie * (r * r);
    cout << "Area of Circle is: " << area;
}